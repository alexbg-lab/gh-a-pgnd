name: 02-Trigger Events

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

  workflow_dispatch:

  schedule:
    - cron: "0 0 * * *"

jobs:
  echo1:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Echo 1"

  echo2:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger      
        run: echo "Triggered by ${{ github.event_name }} event"